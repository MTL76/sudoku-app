{
  "scripts": {
    "dev": "sh -c 'npm run dev --prefix backend & npm run dev --prefix frontend'"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.18",
    "concurrently": "^9.2.1",
    "vitest": "^4.0.18"
  }
}
